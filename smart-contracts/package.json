{
  "name": "greenova8-smart-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Greenova8 investment platform",
  "main": "index.js",
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "test:anchor": "ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts",
    "deploy": "anchor deploy",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "clean": "anchor clean",
    "localnet": "solana-test-validator",
    "logs": "solana logs"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.29.0",
    "@solana/web3.js": "^1.87.6",
    "@solana/spl-token": "^0.3.9"
  },
  "devDependencies": {
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.10.6",
    "chai": "^4.3.10",
    "mocha": "^10.2.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.3.3"
  }
}
